{
  "name": "mirae",
  "packageManager": "yarn@4.8.1",
  "scripts": {
    "dev": "lite-server -c bs-config.js",
    "build": "tsc && cp -r src/*.html src/libs src/assets src/styles dist/ && mkdir -p dist/scripts",
    "vercel-build": "yarn build",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "lite-server": "^2.6.1",
    "typescript": "^5.8.3"
  }
}
